import { createTheme } from '@mui/material';

const themeLight = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#aab8d4',
    },
    secondary: {
      main: '#2dc8ff',
    },
  },
});
export default themeLight;
